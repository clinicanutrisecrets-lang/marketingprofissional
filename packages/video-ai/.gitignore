# A mídia CRUA de datasets/<nome>/ é versionada de propósito enquanto é pequena
# (é o material que a Aline mandou, curado; sem ele ninguém consegue re-treinar).
# O que é DERIVADO fica fora do git: preparado/, frames/, zips/ e qualquer zip.
datasets/**/preparado/
datasets/**/frames/
datasets/**/zips/
datasets/**/*.zip
datasets/**/*.heic
datasets/**/*.heif
outputs/
.env
